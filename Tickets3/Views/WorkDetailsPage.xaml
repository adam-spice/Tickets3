<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Tickets3.Views.WorkDetailsPage">
    <ContentPage.Content>
        <StackLayout Margin="20" VerticalOptions="StartAndExpand">
            <Label Text="Client" />
            <Picker x:Name="clientPicker" HorizontalOptions="FillAndExpand" ItemDisplayBinding="{Binding Name}"  />
            <Label Text="Staff Member" />
            <Picker x:Name="staffPicker" HorizontalOptions="FillAndExpand" ItemDisplayBinding="{Binding Name}" />
            <Button Text="Save" Clicked="OnSaveClicked" />
            <Button Text="Delete" Clicked="OnDeleteClicked" />
            <Button Text="Cancel" Clicked="OnCancelClicked" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>